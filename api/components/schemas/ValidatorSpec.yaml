type: object
required:
  - type
properties:
  type:
    type: string
    description: Validator type (e.g. "notEmpty", "regex")
  params:
    type: object
    description: Parameters for the validator
