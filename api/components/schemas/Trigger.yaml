type: object
properties:
  id:
    type: string
    format: uuid
  name:
    type: string
  type:
    $ref: ./TriggerType.yaml
  workflow_id:
    type: string
    format: uuid
  config:
    $ref: ./TriggerConfig.yaml
  enabled:
    type: boolean
  created_at:
    type: string
    format: date-time
  updated_at:
    type: string
    format: date-time
