packages:
  - 'web'
  - 'packages/*'

# pnpm 10 blocks dependency lifecycle scripts by default. Cypress downloads its
# binary from a postinstall hook, so without this the E2E job fails with
# "the Cypress binary is missing". esbuild also builds on install.
onlyBuiltDependencies:
  - cypress
  - esbuild
